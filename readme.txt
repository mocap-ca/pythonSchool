If you are using the command line version of git and trying to do a commit, you may get the vi editor.

To add the commit message, first press "a" to allow typing, then type the message

When you are done press <ESC> : w q <ENTER> to save

Or <ESC> : q !     to quit